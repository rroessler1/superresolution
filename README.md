Class project to implement a superresolution network.

I only check in a subset of training data here, just as an example.

```
python train.py
python eval.py --path results/
```

You can uncomment the Residual Model in train.py, or can experiment with different downsampling methods in srdataset.py.

